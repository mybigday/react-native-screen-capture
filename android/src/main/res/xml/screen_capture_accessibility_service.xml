<?xml version="1.0" encoding="utf-8"?>
<!--
  Referenced from the host app's manifest:

    <service
        android:name="com.fugood.screencapture.ScreenCaptureAccessibilityService"
        android:exported="false"
        android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>
      <meta-data
          android:name="android.accessibilityservice"
          android:resource="@xml/screen_capture_accessibility_service" />
    </service>

  Copy this file into your own app if you need a different description string.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canTakeScreenshot="true"
    android:description="@string/rn_screen_capture_accessibility_description"
    android:notificationTimeout="100" />
